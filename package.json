{
  "name": "es6-test-package",
  "version": "0.1.2",
  "type": "module",
  "description": "Just testing package stuff",
  "main": "lib/index.js",
  "jsdelivr": "dist/es6-test-package.js",
  "files": [
    "lib/",
    "dist/"
  ],
  "scripts": {
    "test": "node test/index.js",
    "build": "esbuild lib/index.js --bundle --minify --sourcemap --target=es2015 --format=iife --global-name=es6test --outfile=dist/es6-test-package.js"
  },
  "author": "Stefan Peters",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.4"
  },
  "devDependencies": {
    "esbuild": "^0.12.25"
  }
}
